// Prisma client - will be configured when database is set up
// For now, export a placeholder to allow the app to build without a database

// import { PrismaClient } from "@/generated/prisma/client";
// export const prisma = new PrismaClient({ datasourceUrl: process.env.DATABASE_URL });

export const prisma = null as unknown as null;
